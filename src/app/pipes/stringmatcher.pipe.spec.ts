import { StringmatcherPipe } from './stringmatcher.pipe';

describe('StringmatcherPipe', () => {
  it('create an instance', () => {
    const pipe = new StringmatcherPipe();
    expect(pipe).toBeTruthy();
  });
});
